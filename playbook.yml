---
- hosts: all
  become: true

  roles:
    - firewall
    - nginx
    - docker
    - deployments-mhd

  pre_tasks:
    - import_tasks: tasks/pre-task.yml

  post_tasks:
    - import_tasks: tasks/post-task.yml
